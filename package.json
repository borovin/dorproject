{
  "name": "dorproject",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf ./build && node ./tools/r.js -o ./tools/buildConfig.js && ln -snf ./build/ public"
  },
  "dependencies": {
    "requirejs": "^2.1.16"
  }
}

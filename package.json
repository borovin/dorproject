{
  "name": "dorproject",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf ./build && node ./tools/r.js -o ./tools/buildConfig.js && rm -rf ./build/.idea ./build/node_modules ./build/tools"
  },
  "devDependencies": {
    "express": "^4.10.7"
  }
}
